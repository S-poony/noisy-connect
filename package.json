{
  "name": "noisy-connect",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev":   "bun build src/main.ts --outdir dist && bun server.ts",
    "watch": "bun build src/main.ts --outdir dist --watch",
    "build": "bun build src/main.ts --outdir dist --minify"
  },
  "devDependencies": {
    "bun-types": "^1.3.13"
  }
}
